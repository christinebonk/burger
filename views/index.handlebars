<!--Form-->
<form>
	<textarea id="new_burg"></textarea> 
	<button id="submit">Submit</button>
</form>

<!--Burgers-->
<div id="burgers">

	<!--Not Eaten-->
{{!-- 	<div id="not-eaten">
		<ul>
		  {{#each burger}}
		    {{#unless devoured}}
		      {{> burger/burger-block devoured=false}}
		    {{/unless}}
		  {{/each}}
		</ul>
	</div>
 --}}
	<!--Eaten-->
{{!-- 	<div id="eaten">
		{{#each burger}}
		    {{#if devoured}}
		      {{> burger/burger-block devoured=true}}
		    {{/unless}}
		  {{/each}}
		</ul>
	</div>
</div> --}}